import SwiftUI

extension Color {
  /// Matches the app's gold theme color (`colors.gold` in src/theme/theme.ts, #C9A34E).
  static var uppyGold: Color {
    Color(red: 0xC9 / 255.0, green: 0xA3 / 255.0, blue: 0x4E / 255.0)
  }
}
